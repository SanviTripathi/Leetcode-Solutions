class Solution {
    public boolean isPalindrome(int x) {
        int n=x;
        int r=0;
        while(n>0)
        {
            int d=n%10;
             r=10*r+d;
            n=n/10;
        }
        if(x==r)
        return true;
        else 
        return false;
        
    }
}
