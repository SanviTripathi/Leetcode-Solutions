int majorityElement(int* nums, int numsSize) {
    int can;
    int count=0;
    for(int i=0;i<numsSize;i++)
    {
        if(count==0){
        can=nums[i];
        count=1;
        }
        else if(nums[i]==can)
        {
            count++;
        }
        else{
        count--;
        }
    }
    return can;
}
